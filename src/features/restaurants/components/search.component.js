import React from 'react' 

const SearchContainer = styled.View`
  padding: ${(props) => props.theme.space[3]};
`;

export cont Search = () => {
    <SearchContainer>
        <Searchbar />
      </SearchContainer>
}